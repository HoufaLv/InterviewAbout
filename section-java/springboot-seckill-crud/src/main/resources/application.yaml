spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql:///springboot-seckill?useSSL=false
    username: root
    password: rootroot
mybatis:
  configuration:
    map-underscore-to-camel-case: true
  type-aliases-package: com.iw.springbootseckillcrud.entity
  mapper-locations: classpath:mapper/*.xml
pagehelper:
  helper-dialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql
